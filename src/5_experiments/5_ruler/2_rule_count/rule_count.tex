better results the more train rules as shown by table ? (?)

\begin{table}
    \centering
    \input{5_experiments/5_ruler/2_rule_count/results}
    \caption{more train rules = better metrics, evaluated against unknown}
    \label{tab:5_experiments/5_ruler/2_rule_count/results}
\end{table}
